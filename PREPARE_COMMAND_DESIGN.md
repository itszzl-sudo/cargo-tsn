# cargo tsn prepare 命令设计文档

> **版本**: v1.0  
> **创建日期**: 2026-06-05  
> **状态**: 📋 设计阶段

## 一、需求分析

### 1.1 核心需求

**目标**：根据 TypeScript 源码自动生成 FFI 函数的 C 实现文件骨架

**痛点**：
- 手动编写 C 函数实现繁琐且易错
- 需要理解 ts-native 的 NaN-boxing 值表示
- 需要手动处理类型转换（JS ↔ C）
- 容易遗漏 runtime 外部函数声明

### 1.2 用户场景

**场景 1：新项目初始化**
```typescript
// main.ts
declare function crypto_md5(data: string): string;
declare function http_get(url: string): string;

function main() {
    let hash = crypto_md5("hello");
    print(hash);
    return 0;
}
```

**期望输出**：
```bash
$ cargo tsn prepare
📦 Analyzing main.ts...
🔍 Found 2 FFI functions:
  - crypto_md5(string) → string
  - http_get(string) → string

⚙️  Generating C implementations...
  ✓ templates/tsnp/crypto/crypto_win.c
  ✓ templates/tsnp/crypto/ts-native.toml
  ✓ templates/tsnp/crypto/ts-native-win.toml
  ✓ templates/tsnp/http/http_win.c
  ✓ templates/tsnp/http/ts-native.toml
  ✓ templates/tsnp/http/ts-native-win.toml

✅ Prepared 2 plugins with stub implementations
```

---

## 二、可行性评估

### 2.1 方案对比

| 方案 | 可行性 | 优点 | 缺点 | 优先级 |
|------|--------|------|------|--------|
| **A. crates.io 内部 crate** | ⚠️ 部分可行 | 自动化程度高 | 需要 crate 有 FFI 导出 | ⭐⭐⭐ |
| **B. 其他方式（GitHub 扫描）** | ⚠️ 复杂 | 覆盖范围广 | 依赖网络、速度慢 | ⭐⭐ |
| **C. 模板生成** | ✅ 完全可行 | 简单可靠 | 需要手动实现 | ⭐⭐⭐⭐⭐ |

### 2.2 推荐方案：混合策略

**阶段 1**（MVP）：**模板生成** ✅
- 解析 TS 源码提取函数签名
- 生成标准 C 函数骨架
- 生成完整的配置文件
- **可行性：100%**

**阶段 2**（进阶）：**crates.io 集成** ⚠️
- 搜索 crates.io 查找匹配的 crate
- 扫描 crate 的 `#[no_mangle] extern "C"` 函数
- 自动生成映射配置
- **可行性：60%**（依赖 crate 质量）

**阶段 3**（未来）：**GitHub 扫描** ⚠️
- 扫描 GitHub 仓库的 FFI 函数
- 自动适配不同系统库
- **可行性：30%**（复杂度高）

### 2.3 结论

**✅ 可行！采用分阶段实施策略**

- **立即可行**：模板生成（阶段 1）
- **可尝试**：crates.io 集成（阶段 2）
- **暂不实施**：GitHub 扫描（阶段 3）

---

## 三、技术设计

### 3.1 架构设计

```
TypeScript 源码
    ↓
[TS 解析器] (swc)
    ↓
函数签名提取
    ↓
[模板引擎]
    ↓
┌─────────────────┐
│ C 实现骨架      │
│ 配置文件        │
│ 类型声明        │
└─────────────────┘
```

### 3.2 核心流程

```rust
fn cmd_prepare() -> Result<()> {
    // 1. 查找 TS 源码文件
    let ts_files = find_ts_files()?;
    
    // 2. 解析 FFI 函数声明
    let ffi_functions = parse_ffi_declarations(&ts_files)?;
    
    // 3. 按插件分组（根据函数名前缀）
    let plugins = group_by_plugin(&ffi_functions)?;
    
    // 4. 为每个插件生成文件
    for (plugin_name, functions) in plugins {
        generate_plugin(&plugin_name, &functions)?;
    }
    
    // 5. 生成 .ts.toml 依赖声明
    generate_deps_file(&plugins)?;
    
    Ok(())
}
```

### 3.3 TS 解析策略

**方法 1：swc AST 解析**（推荐）✅
```rust
use swc_ecma_parser::{Parser, StringInput};

fn parse_ffi_declarations(files: &[PathBuf]) -> Result<Vec<FFIFunction>> {
    let mut functions = Vec::new();
    
    for file in files {
        let source = fs::read_to_string(file)?;
        
        // 使用 swc 解析
        let ast = parse_ts(&source)?;
        
        // 提取 declare function
        for decl in &ast.body {
            if let Stmt::Decl(Decl::TsDeclare(declare)) = decl {
                if let TsDeclareDecl::TsFnDecl(fn_decl) = &declare.declare {
                    functions.push(FFIFunction {
                        name: fn_decl.ident.sym.to_string(),
                        params: extract_params(&fn_decl.params),
                        return_type: extract_return_type(&fn_decl.return_type),
                    });
                }
            }
        }
    }
    
    Ok(functions)
}
```

**方法 2：正则表达式**（备选）⚠️
```rust
// 简单但不稳定
let re = Regex::new(r#"declare\s+function\s+(\w+)\(([^)]*)\):\s+(\w+)"#)?;
```

### 3.4 插件分组策略

**策略 1：函数名前缀**（推荐）
```typescript
// 自动分组
declare function crypto_md5(...)  → crypto 插件
declare function crypto_sha256(...) → crypto 插件
declare function http_get(...)    → http 插件
declare function http_post(...)   → http 插件
```

**策略 2：用户指定**
```bash
cargo tsn prepare --group crypto:http
```

### 3.5 模板生成

#### C 函数模板

```c
// {plugin}_win.c
// Auto-generated by cargo tsn prepare
// TODO: Implement your function logic

#include <windows.h>

// Runtime external functions
extern double js_string_new(const char* data, unsigned int len);
extern const char* js_string_unpack(double val);
extern double js_number_new(double val);
extern double js_boolean_new(int val);

{function_implementations}
```

**单个函数模板**：
```c
double {func_name}(double {param_vals}) {{
    {param_unpacking}
    
    // TODO: Implement your logic here
    
    {result_packing}
}}
```

**参数解包示例**：
```c
// string 类型
const char* {param_name} = js_string_unpack({param_val});

// number 类型
double {param_name} = {param_val};

// boolean 类型
int {param_name} = (int){param_val};
```

**返回值打包示例**：
```c
// string 返回
return js_string_new(result, len);

// number 返回
return js_number_new(result);

// boolean 返回
return js_boolean_new(result);

// void 返回
return 0;
```

#### 配置文件模板

**ts-native.toml**：
```toml
[package]
name = "tsnp-{plugin}"
version = "0.1.0"
# priority = 0

[includes]
win = "ts-native-win.toml"
linux = "ts-native-linux.toml"
macos = "ts-native-macos.toml"

[signatures]
{signatures}

[build]
warn_on_missing = true
error_on_mismatch = true
```

**ts-native-win.toml**：
```toml
[platform]
name = "Windows"
os = "win"
arch = ["x86_64", "arm64"]
description = "Windows platform"

[libs.default]
description = "Default library"
required = true

[libs.default.functions]
{function_configs}
```

---

## 四、进度通报机制

### 4.1 通报策略

**原则**：耗时长操作必须有及时反馈

**实现**：
```rust
use indicatif::{ProgressBar, ProgressStyle};

fn generate_plugin(plugin: &str, functions: &[FFIFunction]) -> Result<()> {
    let total = functions.len() + 5; // 文件数
    let pb = ProgressBar::new(total as u64);
    pb.set_style(ProgressStyle::default_bar()
        .template("{spinner:.green} [{elapsed_precise}] [{bar:40.cyan/blue}] {pos}/{len} {msg}")
        .progress_chars("#>-"));
    
    pb.set_message(format!("Generating {}", plugin));
    
    // 生成 C 文件
    generate_c_file(plugin, functions)?;
    pb.inc(1);
    
    // 生成配置文件
    generate_toml(plugin, functions)?;
    pb.inc(3);
    
    // 生成类型声明
    generate_dts(plugin, functions)?;
    pb.inc(1);
    
    pb.finish_with_message(format!("✅ {}", plugin));
    
    Ok(())
}
```

### 4.2 输出示例

```bash
$ cargo tsn prepare

📦 Analyzing TypeScript files...
  ✓ Found: main.ts, utils.ts
  ✓ Parsed 5 FFI functions

🔍 Grouping functions by plugin...
  ✓ crypto: 3 functions (md5, sha256, base64)
  ✓ http: 2 functions (get, post)

⚙️  Generating plugins...

crypto ████████████████████████████████ 4/4 
  ✓ templates/tsnp/crypto/crypto_win.c
  ✓ templates/tsnp/crypto/ts-native.toml
  ✓ templates/tsnp/crypto/ts-native-win.toml
  ✓ tsnp/crypto/index.d.ts

http ████████████████████████████ 3/3 
  ✓ tsnp/http/http_win.c
  ✓ tsnp/http/ts-native.toml
  ✓ tsnp/http/index.d.ts

✅ Prepared 2 plugins with 5 functions
📝 Dependencies written to main.ts.toml
```

---

## 五、实施计划

### 5.1 阶段 1：模板生成（MVP）

**目标**：实现基本的 C 文件生成功能

**任务**：
1. ✅ TS 解析（swc）
2. ✅ FFI 函数提取
3. ✅ 插件分组
4. ✅ C 文件模板生成
5. ✅ 配置文件生成
6. ✅ 进度条显示
7. ✅ 依赖文件生成

**预计工作量**：2-3 天

### 5.2 阶段 2：crates.io 集成

**目标**：自动匹配 crates.io 的 crate

**任务**：
1. ⚠️ crates.io API 集成
2. ⚠️ Crate 元数据解析
3. ⚠️ FFI 函数匹配
4. ⚠️ 自动配置生成
5. ⚠️ 回退到模板生成

**预计工作量**：3-5 天
**风险**：中等（依赖 crate 质量）

### 5.3 阶段 3：优化与增强

**目标**：提升用户体验

**任务**：
1. 📋 智能类型推断
2. 📋 代码补全建议
3. 📋 错误检测与提示
4. 📋 增量更新支持
5. 📋 自定义模板

**预计工作量**：2-3 天

---

## 六、风险与缓解

### 6.1 技术风险

| 风险 | 影响 | 概率 | 缓解措施 |
|------|------|------|---------|
| swc 解析失败 | 高 | 中 | 降级到正则解析 |
| crates.io 无匹配 | 中 | 高 | 回退到模板生成 |
| 类型映射错误 | 高 | 低 | 详细的类型检查 |
| 进度条阻塞 | 低 | 低 | 异步更新 |

### 6.2 用户体验风险

| 风险 | 影响 | 缓解措施 |
|------|------|---------|
| 生成时间过长 | 中 | 进度条 + 预估时间 |
| 生成的代码不可用 | 高 | 提供示例和文档 |
| 覆盖用户代码 | 高 | 检查文件存在性 |

---

## 七、测试计划

### 7.1 单元测试

```rust
#[test]
fn test_parse_ffi_declaration() {
    let source = r#"
        declare function crypto_md5(data: string): string;
        declare function http_get(url: string): string;
    "#;
    
    let functions = parse_ffi_declarations(source).unwrap();
    assert_eq!(functions.len(), 2);
    assert_eq!(functions[0].name, "crypto_md5");
}

#[test]
fn test_group_by_plugin() {
    let functions = vec![
        FFIFunction { name: "crypto_md5".into(), .. },
        FFIFunction { name: "crypto_sha256".into(), .. },
        FFIFunction { name: "http_get".into(), .. },
    ];
    
    let groups = group_by_plugin(&functions).unwrap();
    assert_eq!(groups.len(), 2);
    assert!(groups.contains_key("crypto"));
    assert!(groups.contains_key("http"));
}
```

### 7.2 集成测试

```bash
# 测试 1：简单 FFI 函数
cargo tsn prepare --test simple

# 测试 2：多插件
cargo tsn prepare --test multi-plugin

# 测试 3：复杂类型
cargo tsn prepare --test complex-types
```

---

## 八、命令接口设计

### 8.1 CLI 设计

```bash
cargo tsn prepare [OPTIONS]

Options:
  --input <FILE>         指定输入文件（默认：自动扫描 *.ts）
  --output <DIR>         输出目录（默认：tsnp/）
  --group <STRATEGY>     分组策略：prefix（默认）, manual
  --template <NAME>      使用自定义模板
  --dry-run              预览生成内容但不写入文件
  --verbose              详细输出
  --quiet                仅显示错误
```

### 8.2 使用示例

**基本用法**：
```bash
cargo tsn prepare
```

**指定输入**：
```bash
cargo tsn prepare --input src/main.ts
```

**预览模式**：
```bash
cargo tsn prepare --dry-run
```

**详细输出**：
```bash
cargo tsn prepare --verbose
```

---

## 九、文件输出结构

```
tsnp/
├── crypto/
│   ├── crypto_win.c           ← 自动生成
│   ├── crypto_linux.c         ← 占位
│   ├── crypto_macos.c         ← 占位
│   ├── ts-native.toml         ← 自动生成
│   ├── ts-native-win.toml     ← 自动生成
│   ├── ts-native-linux.toml   ← 自动生成
│   ├── ts-native-macos.toml   ← 自动生成
│   └── index.d.ts             ← 自动生成
├── http/
│   └── ... (同上)
└── ...
```

---

## 十、与现有工具集成

### 10.1 与 tsnp 的关系

> **注意**：`tsnp` 工具已被 `cargo tsn prepare` 替代。

| 工具 | 职责 | 输入 | 输出 | 状态 |
|------|------|------|------|------|
| **cargo tsn new** | 创建空项目 | 项目名 | 项目脚手架 | ✅ 推荐 |
| **cargo tsn prepare** | 从 TS 源码生成插件 | TS 文件 | C 实现 + 配置 | ✅ 推荐 |
| **tsnp new** | 创建空插件模板 | 插件名 | 空模板文件 | ⚠️ 已废弃 |
| **tsnp gen** | 从 crate 生成配置 | crate 名 | 配置文件 | ⚠️ 已废弃 |

### 10.2 工作流

```
1. 编写 TS 代码（声明 FFI 函数）
   ↓
2. cargo tsn prepare（生成 C 骨架）
   ↓
3. 手动实现 C 函数逻辑
   ↓
4. tsn build（编译）
   ↓
5. 运行测试
```

---

## 十一、总结

### ✅ 可行性结论

**完全可行！建议立即实施阶段 1（模板生成）**

**理由**：
1. ✅ 技术栈成熟（swc 已在项目中使用）
2. ✅ 需求明确（开发者痛点）
3. ✅ 实施风险低（模板生成可控）
4. ✅ 渐进式实施（可后续增强）

### 📋 下一步行动

1. **创建实现分支**
2. **实现 TS 解析模块**
3. **实现模板引擎**
4. **实现进度通报**
5. **编写测试用例**
6. **集成到 cargo-tsn**
7. **文档更新**

### ⏱️ 预计时间线

- **Week 1**：核心功能开发（阶段 1）
- **Week 2**：测试与优化
- **Week 3**：crates.io 集成尝试（阶段 2）
- **Week 4**：文档与发布

---

**文档版本**: v1.0  
**最后更新**: 2026-06-05  
**状态**: 📋 待评审  
**作者**: AI Assistant
